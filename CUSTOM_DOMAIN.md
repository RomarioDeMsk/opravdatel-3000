# Подключение своего домена к сайту

## Обзор

После размещения сайта на GitHub Pages, Netlify, Vercel или другом хостинге, вы можете подключить свой домен (например, `opravdatel3000.ru` или `opravdatel3000.com`).

---

## Вариант 1: GitHub Pages + Свой домен

### Шаг 1: Настройте домен на GitHub

1. **Откройте репозиторий на GitHub**
2. **Перейдите в Settings → Pages**
3. **В разделе "Custom domain" введите свой домен**
   - Например: `opravdatel3000.ru` или `www.opravdatel3000.ru`
   - Нажмите "Save"
4. **GitHub создаст файл CNAME автоматически** (или создайте его вручную)

### Шаг 2: Создайте файл CNAME (если не создан автоматически)

В корне репозитория создайте файл `CNAME` (без расширения) с содержимым:

```
opravdatel3000.ru
```

Или для поддомена www:
```
www.opravdatel3000.ru
```

Загрузите файл на GitHub:
```bash
# Создайте файл CNAME
echo "opravdatel3000.ru" > CNAME

# Добавьте и закоммитьте
git add CNAME
git commit -m "Add custom domain"
git push
```

### Шаг 3: Настройте DNS записи у регистратора домена

Перейдите к вашему регистратору домена (например, REG.RU, Timeweb, GoDaddy, Namecheap) и настройте DNS:

#### Вариант A: Только домен (без www)

**A записи** (IPv4):
```
Тип: A
Имя: @ (или оставьте пустым)
Значение: 185.199.108.153
TTL: 3600 (или Auto)

Тип: A
Имя: @
Значение: 185.199.109.153
TTL: 3600

Тип: A
Имя: @
Значение: 185.199.110.153
TTL: 3600

Тип: A
Имя: @
Значение: 185.199.111.153
TTL: 3600
```

#### Вариант B: С www

**A записи** (для основного домена):
```
@ → 185.199.108.153
@ → 185.199.109.153
@ → 185.199.110.153
@ → 185.199.111.153
```

**CNAME запись** (для www):
```
Тип: CNAME
Имя: www
Значение: YOUR_USERNAME.github.io
TTL: 3600
```

### Шаг 4: Подождите активации

- DNS изменения могут занять от нескольких минут до 48 часов
- Обычно активация происходит в течение 1-2 часов
- Проверить можно командой: `nslookup opravdatel3000.ru`

### Шаг 5: Включите HTTPS (автоматически)

GitHub Pages автоматически предоставляет SSL сертификат через Let's Encrypt. После настройки DNS:
1. Вернитесь в Settings → Pages
2. Убедитесь, что галочка "Enforce HTTPS" включена (появится через некоторое время)

---

## Вариант 2: Netlify + Свой домен

### Шаг 1: Добавьте домен в Netlify

1. **Откройте проект в Netlify**
2. **Перейдите в Domain settings**
3. **Нажмите "Add custom domain"**
4. **Введите ваш домен** (например, `opravdatel3000.ru`)
5. **Нажмите "Verify"**

### Шаг 2: Настройте DNS

Netlify покажет вам нужные DNS записи. Обычно это:

**Вариант A: Netlify DNS (рекомендуется)**
- Измените NS записи у регистратора на те, что даст Netlify
- Netlify будет управлять всеми DNS записями

**Вариант B: Собственный DNS**
- Добавьте A запись:
  ```
  @ → 75.2.60.5
  ```
- Или CNAME:
  ```
  @ → your-site.netlify.app
  ```

### Шаг 3: SSL сертификат

Netlify автоматически выдаст SSL сертификат через Let's Encrypt (обычно в течение 24 часов).

---

## Вариант 3: Vercel + Свой домен

### Шаг 1: Добавьте домен

1. **Откройте проект в Vercel**
2. **Перейдите в Settings → Domains**
3. **Нажмите "Add"**
4. **Введите домен** и следуйте инструкциям

### Шаг 2: Настройте DNS

Vercel покажет нужные DNS записи. Обычно:

**A запись:**
```
@ → 76.76.21.21
```

**Или CNAME:**
```
@ → cname.vercel-dns.com
```

---

## Вариант 4: Cloudflare Pages + Свой домен

### Шаг 1: Добавьте домен

1. **В Cloudflare Pages откройте проект**
2. **Перейдите в Custom domains**
3. **Нажмите "Set up a custom domain"**
4. **Введите домен**

### Шаг 2: Настройте DNS

Если домен уже в Cloudflare:
- DNS записи настроятся автоматически

Если домен не в Cloudflare:
- Добавьте CNAME запись:
  ```
  @ → your-project.pages.dev
  ```

---

## Пошаговая инструкция для популярных регистраторов

### REG.RU

1. Войдите в личный кабинет
2. Перейдите в "Домены" → выберите домен → "DNS-серверы и зона"
3. Выберите "Использовать DNS-серверы REG.RU"
4. Добавьте A записи (см. выше для GitHub Pages)
5. Сохраните изменения

### Timeweb

1. Войдите в панель управления
2. Перейдите в "Домены" → выберите домен → "DNS-записи"
3. Добавьте A записи
4. Сохраните

### GoDaddy

1. Войдите в аккаунт
2. Перейдите в "My Products" → "DNS"
3. Добавьте A записи в разделе "Records"
4. Сохраните

### Namecheap

1. Войдите в аккаунт
2. Перейдите в "Domain List" → выберите домен → "Advanced DNS"
3. Добавьте A записи
4. Сохраните

---

## Проверка настройки DNS

### Через командную строку:

```bash
# Windows PowerShell
nslookup opravdatel3000.ru

# Или через онлайн-сервисы:
# https://dnschecker.org
# https://www.whatsmydns.net
```

### Что должно быть:
- A записи должны указывать на IP адреса GitHub (185.199.108.153 и т.д.)
- CNAME записи должны указывать на ваш хостинг

---

## Важные моменты

### 1. Время активации DNS
- Обычно: 15 минут - 2 часа
- Максимум: до 48 часов
- Проверяйте через `nslookup` или онлайн-сервисы

### 2. HTTPS сертификат
- GitHub Pages: автоматически через 1-24 часа
- Netlify: автоматически через несколько часов
- Vercel: автоматически через несколько минут
- Cloudflare: автоматически

### 3. Поддомен www
- Можно использовать как с www, так и без
- Рекомендуется настроить редирект (обычно настраивается автоматически)

### 4. Обновление сайта
- После подключения домена обновление сайта работает так же
- Просто делайте `git push` (для GitHub) или загружайте файлы

---

## Решение проблем

### Домен не открывается
1. Проверьте DNS записи через `nslookup`
2. Убедитесь, что прошло достаточно времени (до 48 часов)
3. Проверьте, что домен добавлен в настройках хостинга
4. Очистите кэш DNS: `ipconfig /flushdns` (Windows)

### HTTPS не работает
1. Подождите до 24 часов (сертификат выдается автоматически)
2. Убедитесь, что DNS записи настроены правильно
3. Проверьте, что домен подтвержден на хостинге

### Ошибка "Domain not found"
1. Проверьте правильность написания домена
2. Убедитесь, что домен добавлен в настройках хостинга
3. Проверьте DNS записи

---

## Примеры популярных регистраторов в России

- **REG.RU** - https://www.reg.ru
- **Timeweb** - https://timeweb.com
- **2domains** - https://2domains.ru
- **Beget** - https://beget.com
- **Jino** - https://jino.ru

---

## Готово! 🎉

После настройки DNS и активации SSL ваш сайт будет доступен по адресу:
- `https://opravdatel3000.ru`
- `https://www.opravdatel3000.ru` (если настроили)

Обычно весь процесс занимает 1-24 часа.

