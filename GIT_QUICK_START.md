# Быстрый старт с Git для размещения сайта

## Правильная настройка Git

### 1. Настройте имя и email

```bash
# Правильная команда (обратите внимание на кавычки и ключи!)
git config --global user.name "RomarioDeMsk"
git config --global user.email "romario@example.com"
```

**Важно:**
- ✅ `user.name` и `user.email` — это ключи (обязательно!)
- ✅ Значения должны быть в кавычках
- ✅ Email может быть любым (даже несуществующим)

### 2. Проверьте настройки

```bash
git config --global --list
```

Должно показать:
```
user.name=RomarioDeMsk
user.email=romario@example.com
```

---

## Размещение сайта на GitHub

### Шаг 1: Перейдите в папку с сайтом

```bash
cd d:/BABKI/web/static
```

### Шаг 2: Инициализируйте Git репозиторий

```bash
git init
```

### Шаг 3: Добавьте все файлы

```bash
git add .
```

### Шаг 4: Сделайте первый коммит

```bash
git commit -m "Initial commit: Оправдатель 3000"
```

### Шаг 5: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "+" → "New repository"
3. Название: `opravdatel-3000`
4. Выберите "Public"
5. НЕ добавляйте README, .gitignore, лицензию
6. Нажмите "Create repository"

### Шаг 6: Подключите к GitHub

```bash
# Замените YOUR_USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR_USERNAME/opravdatel-3000.git
git branch -M main
git push -u origin main
```

Вас попросят ввести логин и пароль GitHub (или токен доступа).

### Шаг 7: Включите GitHub Pages

1. На GitHub откройте репозиторий
2. Settings → Pages
3. Source: "Deploy from a branch"
4. Branch: `main`, Folder: `/ (root)`
5. Save

### Готово! 🎉

Сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/opravdatel-3000/`

---

## Частые ошибки и решения

### Ошибка: "key does not contain a section"
**Причина:** Неправильный синтаксис команды `git config`

**Правильно:**
```bash
git config --global user.name "RomarioDeMsk"
```

**Неправильно:**
```bash
git config --global RomarioDeMsk  # ❌ Отсутствует ключ user.name
```

### Ошибка: "fatal: not a git repository"
**Решение:** Выполните `git init` в папке с сайтом

### Ошибка: "Permission denied"
**Решение:** Проверьте правильность URL репозитория и учетные данные

---

## Альтернатива: Без Git

Если Git вызывает проблемы, используйте **Netlify Drag & Drop**:
1. Откройте https://www.netlify.com
2. Перетащите папку `web/static` на сайт
3. Готово!

См. файл `DEPLOY_WITHOUT_GIT.md` для подробностей.

